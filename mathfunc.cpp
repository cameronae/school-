#include <iostream>
#include <cmath>

int main()
{
    double x;
    double y;
    double z;
    double a;
   std::cout << "enter x";
   std::cin >> x;
    //z = std::max(x,y); finds the max in func
    //a = std::min(x,y); finds the min in func
    //z = pow(x, y); sets variable to a power
    //z = sqrt(x); gets the square root of a number
    //z = round(x); rounds a num 
    //z = ceil(x); rounds a number up
    //z = floor(x) rounds a number down
}